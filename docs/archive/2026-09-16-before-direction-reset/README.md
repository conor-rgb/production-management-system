# Historical documents before the direction reset

Preserved 16 September 2026. These files are historical evidence, not current implementation instructions or deployment claims. Start at [the current documentation index](../../README.md).

Original Markdown bytes are preserved. `manifest.json` records original path, archive path, source and SHA-256. Files already deleted before this session were recovered from Git HEAD into `previously-deleted/`; they were not silently restored as active policy. Historical agent guidance uses `.previous.md` filenames.

The 4,398-line handover remains complete in `HANDOVER.md`. Its old “next steps”, storage bans, feature promises and deployment instructions are superseded by the current brief, plan and release records. Historical counts and export reports describe their original capture dates, not today's database.
